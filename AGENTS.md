# 项目协作说明

## 项目概览

这是一个中文单页待办事项应用，页面标题为“今日清单 · Todo List”。项目是零构建步骤的静态前端：直接在浏览器中打开 `index.html` 即可运行，不依赖包管理器、后端服务或测试框架。

## 文件与职责

| 文件 | 职责 |
| --- | --- |
| `index.html` | 页面语义结构、可访问性属性、外部字体和 CSS/JS 引用。 |
| `app.js` | 任务状态、渲染、筛选、编辑、浏览器本地存储，以及空状态鼓励语逻辑。 |
| `styles.css` | 主题变量、响应式布局、组件状态和交互样式。 |

## Code Review Rules

- 任务文本插入 HTML 前必须经过 `escapeHtml()`，不可把用户输入直接作为 HTML 执行。
- 每次修改任务状态后必须通过 `render()` 同步 DOM 和本地存储。
- 保持简体中文文案、`lang="zh-CN"`、键盘可用的原生按钮和 520px 移动端布局。
- 修复必须范围最小；不要无故加入构建工具、框架或依赖。
- 修改后运行 `node scripts/verify.mjs` 与 `node --check app.js`。
